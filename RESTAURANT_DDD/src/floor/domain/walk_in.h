#pragma once

namespace floor_ctx {

// Walk-in — a party that arrives without a reservation.
// Sara tracks these differently from reservations.
struct WalkIn {
    int partySize;
};

} // namespace floor_ctx
