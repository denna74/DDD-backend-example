services:
  restaurant:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8081:8081"
      - "8082:8082"
    volumes:
      - db-data:/app/data

volumes:
  db-data:
